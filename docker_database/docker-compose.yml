services:
  web:
    build: ./db
    image: colepatrick/herculean-db:latest
    ports:
      - "5000:5000"
    environment:
      REDIS_URL: redis://redis:6379
    depends_on:
      - redis

  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
    
volumes:
  redis-data: